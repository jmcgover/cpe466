#include "graph_parse.h"

#include <stdlib.h>

char *parse_graph_num_line(char *beg_line, graph_num_record_t *record) {
    char *cur = NULL;
    if (beg_line == NULL) {
        return beg_line;
    }
    return cur;
}
